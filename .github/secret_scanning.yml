# Exclude the committed, throwaway E2E TLS fixture from GitHub secret scanning.
# Test-only localhost credentials for the GreenMail IMAPS fixture; trusted only
# by a debug build via COSMIC_MAIL_EXTRA_CA. See e2e/fixtures/tls/generate.sh.
paths-ignore:
  - "e2e/fixtures/tls/**"
